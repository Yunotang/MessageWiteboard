# Project Context and Instructions

## Deployment Notes
- **GitHub Pages**: The app is being deployed to `yunotang.github.io`.
- **Firebase Authentication**: For Google Login to work on the deployed GitHub Pages site, `yunotang.github.io` MUST be added to the Firebase Console -> Authentication -> Settings -> Authorized domains list. Otherwise, the Google login popup will be blocked by Firebase.
